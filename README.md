An MPC motion planner for mobile robots in dynamic environments.

- Install required python-packages
```
pip install -r requirements.txt
```
- Run an example with the planner in a dynamic environment:
```
python example.py  
```
